
<h1>Edit Image</h1>

<%= form_for([@gallery, @image]) do |form| %>
  <%= render form %>
  <%= form.submit "Update image" %>
<% end %>

<div>
  <%= check_box_tag :tag_ids, "@image.tags, :id, :name" %>
</div>